/* Tailwind v4 uses `app/globals.css` (@import "tailwindcss" + @theme) as the
 * source of truth. This file is kept so `components.json` and tooling that
 * expect a config path can resolve. */
export default {};
